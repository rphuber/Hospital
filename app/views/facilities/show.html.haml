%h2
  = @facility.name
= link_to 'Back to List', facilities_path

%br

%h3 Patient List

%ul
  - @patients.each do |patient| 
    %li
      %p
        = patient.first_name
        = patient.last_name 

        = link_to "View Details", patient_path(patient), class: "button tiny" 
        = link_to "Edit", edit_patient_path(patient), class: "button tiny"

        = link_to "Delete", patient_path(patient), method: :delete, class: "button tiny alert" 


        %br
      




= link_to 'Add Patient', new_facility_patient_path(@facility)

%hr
